import Panel from '@ui/panel';
import InspectableControl from '../inspectable';

export default class Inspector extends Panel {
	constructor() {
		super('Inspector');
	}

	Clear() {}

	Inspect(inspectable: InspectableControl) {
		//
	}
}
